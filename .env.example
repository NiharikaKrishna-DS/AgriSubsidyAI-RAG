text# LLM Provider selection (e.g., openai, anthropic, ollama)
LLM_PROVIDER=
# Embedding Provider selection (e.g., openai, huggingface)
EMBEDDING_PROVIDER=
# Hugging Face embedding model used for embeddings and token counting
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# Local path to your database
DB_PATH=./data/database/agrisubsidy.db
# Add your API Key here locally (Do not commit real keys!)
OPENAI_API_KEY=
HF_TOKEN=
